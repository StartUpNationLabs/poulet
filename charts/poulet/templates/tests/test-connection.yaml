apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "poulet.fullname" . }}-test-connection"
  labels:
    {{- include "poulet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "poulet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
